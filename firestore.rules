/**
 * DEVELOPMENT ONLY - INSECURE RULES
 * This ruleset allows any authenticated user to read and write to the entire database.
 * It is intended for development purposes only to get unblocked from permission errors.
 *
 * WARNING:
 * Before deploying to a production environment, you MUST replace these rules with
 * secure rules that properly restrict access and protect user data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access to any authenticated user.
      allow read, write: if request.auth != null;
    }
  }
}
