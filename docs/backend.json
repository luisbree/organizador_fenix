{
  "entities": {
    "TaskList": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task List",
      "type": "object",
      "description": "Represents a list of tasks.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the TaskList entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the task list."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the task list was created."
        }
      },
      "required": ["id", "name", "createdAt"]
    },
    "Task": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task",
      "type": "object",
      "description": "Represents a task with its details, index, and completion status.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Task entity."
        },
        "tarea": {
          "type": "string",
          "description": "Textual description of the task."
        },
        "urgencia": {
          "type": "number",
          "description": "Numerical value representing the urgency of the task."
        },
        "necesidad": {
          "type": "number",
          "description": "Numerical value representing the necessity of the task."
        },
        "costo": {
          "type": "number",
          "description": "Numerical value representing the cost of the task."
        },
        "duracion": {
          "type": "number",
          "description": "Numerical value representing the duration of the task."
        },
        "indice": {
          "type": "number",
          "description": "Calculated index of the task, used for sorting."
        },
        "completado": {
          "type": "boolean",
          "description": "Boolean value indicating whether the task is completed or not."
        },
        "completedAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp when the task was completed."
        },
        "isFenix": {
          "type": "boolean",
          "description": "Indicates if the task should automatically be reborn after completion."
        },
        "fenixPeriod": {
          "type": "number",
          "description": "The period in days after which a Fenix task should be reborn."
        },
        "isCritical": {
          "type": "boolean",
          "description": "Indicates if the task is critical and should appear at the top."
        }
      },
      "required": [
        "id",
        "tarea",
        "urgencia",
        "necesidad",
        "costo",
        "duracion",
        "indice",
        "completado"
      ]
    },
    "SubTask": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SubTask",
      "type": "object",
      "description": "Represents a sub-task linked to a parent task.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SubTask entity."
        },
        "parentId": {
          "type": "string",
          "description": "The ID of the parent Task."
        },
        "tarea": {
          "type": "string",
          "description": "Textual description of the sub-task."
        },
        "completado": {
          "type": "boolean",
          "description": "Boolean value indicating whether the sub-task is completed or not."
        }
      },
      "required": ["id", "parentId", "tarea", "completado"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/taskLists/{listId}",
        "definition": {
          "entityName": "TaskList",
          "schema": {
            "$ref": "#/entities/TaskList"
          },
          "description": "Stores the user's task lists."
        }
      },
      {
        "path": "/users/{userId}/taskLists/{listId}/tasks/{taskId}",
        "definition": {
          "entityName": "Task",
          "schema": {
            "$ref": "#/entities/Task"
          },
          "description": "Stores tasks for a specific list."
        }
      },
      {
        "path": "/users/{userId}/taskLists/{listId}/tasks/{taskId}/subtasks/{subtaskId}",
        "definition": {
          "entityName": "SubTask",
          "schema": {
            "$ref": "#/entities/SubTask"
          },
          "description": "Stores sub-tasks for a specific task."
        }
      }
    ]
  }
}
